[project]
name = "omni-host-bridge"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "python-multipart>=0.0.20",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
