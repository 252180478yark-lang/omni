"""投放复盘 prompt 共用常量 & 组件。

所有 review / file_parser 的硬约束集中在此，保证跨 prompt 措辞一致、
规则不重复（不再出现"数据第一原则"被复述三遍的情况）。
"""

from __future__ import annotations

# ═══════════════════════════════════════════════════════════
# 核心纪律 — 最高优先级，所有 prompt 都必须遵守
# ═══════════════════════════════════════════════════════════

FACTUAL_DISCIPLINE = """## 核心纪律（最高优先级，违反即视为不合格）

1. **数据纪律**：所有结论必须直接来自输入数据或视频分析报告。禁止引入未提供的指标、行业均值、推测数字。
2. **已提供字段提醒**（已在输入中完整给出，不要误判为"数据缺失"）：
   - `conversion_rate`：种草计划即 A3 转化率，可 >100%（归因窗口导致属正常）
   - `cost_per_result`：种草计划即 A3 成本
   - 视频分析全维度：评分、钩子、画面、BGM、脚本、表演、场景、改进建议
3. **结论格式**：每条结论必须包含【结论】+【证据(具体数据或视频内容)】+【判定(有效/无效/不确定)】+【置信度(高/中/低)】。
4. **无证据就声明无证据**：若某分析所需数据不在输入中，对应字段输出 `null` 并附 `_reason`，或在 Markdown 中写"数据不足：缺少 XX 字段"。禁止用"可能/一般/大概率"替代证据。"""


RELEASE_MODE_CONSTRAINT = """## 放量投放硬约束

当前投放模式为**放量投放**，系统自动竞价。
- **禁止的建议类型**：降低出价、提高出价、调整预算分配、控制日消耗、调整出价策略。
- **仅允许两个优化方向**：① 素材内容迭代 ② 人群包选择与匹配。
- 归因亏损时**禁止将 CPM 作为原因**——CPM 是 eCPM 的结果，真正原因必须追溯到 CTR（表层不匹配）或 CVR（深层不匹配）。"""


TRIANGLE_MATCHING_FRAMEWORK = """## 分析框架：素材×人群×目的 三角匹配

### eCPM 竞价公式
`eCPM = CTR × CVR × 出价 × 1000`。平台按 eCPM 排序分配曝光。
- **CTR = 表层匹配度**：素材钩子/封面 ↔ 人群即时兴趣
- **CVR = 深层匹配度**：素材说服逻辑 ↔ 人群决策因素
- **CPM = eCPM 的结果**，不是原因

### 4 象限匹配诊断
| CTR | CVR | 诊断 | 行动方向 |
|---|---|---|---|
| 高 | 高 | 表层+深层都匹配 | 放量追加 |
| 高 | 低 | 钩子吸引了人但内容没说服 / 吸引了错的人 | 改中后段内容 或 换人群 |
| 低 | 高 | 能转化但封面不吸引人 | 改钩子/封面，内容保持 |
| 低 | 低 | 彻底错配 | 换素材方向 或 换人群 |

### 三类素材的指标优先级
- **种草**（A3 量目标）：CTR → A3 转化率(=conversion_rate) → eCPM 竞争力 → A3 成本(=cost_per_result)
- **转化**（ROI 目标 oCPM）：CTR → CVR → eCPM 竞争力 → 单转化成本 vs 保本线
- **自然流量**（主账号，不走千川计费）：3 秒率 → 5 秒率 → 完播率 → 播放量 → 互动率

### 归因三步法
1. **对齐**：同一变量组内只改变一个因素做对照（同风格组内改钩子 vs 不改的）
2. **定位**：数据变化归因到具体视频秒级变更（如"第 2 秒换产品特写 → 3 秒率提升"）
3. **判定**：有效 / 无效 / 不确定 + 信心等级（高 / 中 / 低）"""


KB_CITATION_RULE = """## 知识库使用规则
下方"历史投放经验"来自知识库。分析时可引用相关方法论支撑诊断（注明来源，如"根据千川官方 XX 策略"），但**证据权重始终是：输入数据 > 知识库内容**——知识库不能推翻输入数据的事实。"""


# ═══════════════════════════════════════════════════════════
# 与 knowledge-engine 保持一致的 "辅助参考" 角色规则
# （此处为跨服务同步副本，措辞需与 knowledge-engine 的 KB_AS_SUPPORT 对齐）
# ═══════════════════════════════════════════════════════════

KB_AS_SUPPORT = """## 知识库使用规则（角色：辅助参考）

1. **证据权重**：用户输入的数据（指标、视频分析、产品信息）>> 知识库内容 >> 你的常识。
2. **知识库不得推翻数据**：若知识库经验与输入数据冲突，以数据为准。写法："数据呈现 A；知识库经验是 B；可能原因：……"。
3. **引用格式**：
   - 引用知识库：`[KB:{source}#{id}]`
   - 引用输入数据：用字段名，如 `(依据：CTR=2.1%, CVR=0.3%)`
   - 自己的推断：`（推断：基于 XX 逻辑）`
4. **缺失处置**：若知识库无召回，直接忽略这部分，不需专门声明（输入数据已足够支撑分析）。"""


def format_kb_snippets(snippets: list[dict]) -> str:
    """把检索结果渲染为统一的 XML <kb_context> 段。

    与 knowledge-engine 保持格式一致（source / id / score / content）。
    空输入时返回明确的"无召回"标记。
    """
    if not snippets:
        return '<kb_context empty="true" note="本次检索无召回结果" />'

    lines = ["<kb_context>"]
    for i, s in enumerate(snippets, start=1):
        source = s.get("source") or s.get("kb_name") or "unknown"
        snippet_id = str(s.get("id") or s.get("chunk_id") or i)
        score = float(s.get("score") or 0)
        content = (s.get("content") or "").strip()
        if not content:
            continue
        content = content.replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")
        lines.append(
            f'  <snippet source="{source}" id="{snippet_id}" score="{score:.2f}">\n'
            f'    {content}\n'
            f'  </snippet>'
        )
    lines.append("</kb_context>")
    return "\n".join(lines)
