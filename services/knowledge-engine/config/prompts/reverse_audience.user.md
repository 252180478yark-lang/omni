## 反推任务

请对附件竞品视频（已通过 Gemini Files API 上传给你）做**人群逆向分析**——它在打什么人。
不是拍摄反推：不要输出分镜、镜头语言、生成模型 prompt。

---

## 竞品提示（老板给的定锚，可能为空）

{competitor_hint_block}

---

## 老板临时方向

{extra_context_block}

---

## 输出要求

1. **严格 JSON 输出**（response_mime_type 已设 application/json），不要围栏、不要寒暄
2. 先逐项过完 8 项信号清单再写假设；编不出的信号写空字符串/空数组，不要硬填
3. persona_md / funnel_guess 里**每句判断标 🧠（写明依据信号）或 ⚠️**；**所有数字必带 ⚠️**
4. **禁止 [KB:] 标记**（你没有任何 KB 依据，只有这条视频）
5. 核心服务人群假设 1-3 个，宁缺毋滥；说人话，禁 AI 套话

直接输出 JSON。
