## ① 输入信息

### SKU
{sku_md}

### 卖点矩阵（来自 step 2，节号沿用）
{matrix_md}

### 选中的人群（来自 step 3 拆出，老板已勾选）

**人群名**：{audience_name}
**KB 来源**：{audience_kb_doc}{audience_kb_section_suffix}
**圈层标签**：{audience_layer_tags}

**KB 原文**：

{audience_kb_chunk}

**5 条匹配理由**（卖点 + 场景）：

{audience_match_reasons_md}

### 老板临时要求（extra_context）
{extra_context}

---

## ② 巨量云图 + 巨量千川 官方 KB 召回（**核心权威依据**）

下面是从巨量云图 + 巨量千川官方 KB 召回的 chunks。**第 2/3 部分的标签和定向方式必须严格用 KB 里出现的真实标签 / 圈选规则，不要发明虚构标签**。

每条标签的引用理由要 quote 这里的 KB 段（带 [巨量云图 - 文档名] 或 [巨量千川 - 文档名] 来源）。

**优先级提醒**：召回段里若出现 doc **"巨量云图标签体系 完整结构化字典 v2"** 或
**"巨量云图 自定义人群创建页 完整标签字典 v1"**，**它们是标签层级的最权威真清单**，
标签命名/上下级严格照搬不许改写。涉及"买过/看过某品类·品牌"的圈人，**必须用 v2 里的
电商品类成交偏好二级路径（如 `粮油米面南北干货调味品 > 食用油调味油`）+ 走数据工厂标签工厂建
商品人群标签**（见 system §4.5.0 / §4.5.4），不要用自定义人群直接勾（那里没有这棵树）。

{platform_kb_context}

---

## ③ 你的任务

按 system 里七节的固定结构输出**完整圈包 SOP**。

要点提醒：
1. **第 0 部分**：4 维度合理推理（生活方式 / 消费习惯 / 痛点 / 触发场景），每句标 [KB] / [matrix X.Y] / [行业推理]，信息不足写"建议老板补 X"不脑补
2. **第 1 部分**：1.1 概览表（一句话目标 + 最准/辅助/不适用方式 + 理由）+ 1.2 ASCII 圈人架构图（前置工具 → N 个圈人单元 → 组合关系 ∩∪- → 推千川 + 末尾"整个流程是这样"回顾段）—— **两块都必给**
3. **第 2 部分**：N 个圈人单元（**N 由你判断、无上下限**），每个单元颗粒度细到三级菜单 + 具体可选项 + 大白话理由 + 末尾「跟其他单元/最终圈包的关系」对应 1.2 架构图；**严禁虚构 KB 没出现的标签名**（IP 偏好只 5 类、内容偏好/行业品类兴趣三级树没给清单时写"建议老板在云图后台 XX 菜单查实际可选项"）
4. **第 3 部分**：交并排拓配方 — **只在 1 个单元不够精准时给**（数量按真需要，可以 0 / 1 / 多套，**禁止凑数**）；每套必含「公式 + 操作 SOP + 推千川 + 为什么这种运算 + 每个被组合单元的贡献 + 整体目标达成」
5. **第 4 部分**：按 system 4.7 判定表 — 用了 generate_keyword_pack 写情况 A（种子词 + 4 步链路：关键词夹 → 标签工厂 → 自定义人群引用 → 推千川），不用就写情况 B 一句话理由后跳过

**严禁越界**：
- ❌ 不写脚本钩子文案 / 不预测 ROI/GMV / 不推计划类型 / 不重写 KB 原文
- ❌ **不写预算（测试期/放量期日预算）/ 不写投放计划设计 / 不写 A/B 测试矩阵 / 不写 P0/P1/P2 优先级** —— 这些都是老板自己拍的事，不归你管
- ❌ 不用「8 大维度标签 SOP」当一级章节 —— 维度是原料，藏在自定义人群勾选步骤里
- ❌ 不把"关键词"误指到千川计划关键词定向 —— 关键词永远走云图数据工厂关键词夹
