## ① 产品基本信息

{sku_md}

## ② 卖点矩阵（step 2 输出 — 含三层卖点 + 5 心智 + 标签汇总）

{matrix_md}

## ③ KB 召回人群片段（多 query 多角度 + 全 KB doc 扩散，合并去重）

> 这是 tool 内部按 **品类锚 + matrix_md 卖点/场景/心智 + 全 KB 46 个 doc** 多角度 query 对人群分析报告 KB 召回的 chunks，**合并去重 + doc round-robin 重排**的结果（让每个 doc 都有公平召回机会）。
> 不是单 query 召回（避免单 query 偏移）。**所有 chunks 都要过一遍**，每段问"这个人群跟 matrix 三要素对得上吗"，对得上就挑出来。

{kb_recall}

## ④ 额外要求

{extra_context}

---

请按 system prompt **第六节**「输出结构」的 **2 部分** 正式输出（第 1 部分 KB 匹配人群 **≥ 15 + 跨 ≥ 10 个 doc** + 第 2 部分 标签汇总 ≥ 30）。KB 总共 **46 个 doc**，候选池 80 chunks 跨 30+ doc，挑出 ≥15 个对口人群跨 ≥10 doc 完全可达。

**输出前**先用一小段话做**资料完备性诊断 + 补料建议**：
- matrix_md 是否充分？（卖点节号是否清晰？场景是否具体？心智维度是否齐全？）
- KB 召回涉及哪些 doc？（看上面 ③ 的 doc 来源清单）
- 哪些维度建议老板补料？给具体的资料类型（如「巨量算数 X 报告」「小红书 X 决策路径」）

**禁止列"人群假设清单 / 假设 0.X"**——直接拿 matrix 三要素去 KB chunks 里挑对口人群。

诊断完直接进入第 1 部分。**严格按 system prompt 第九节自检清单逐条核对**，不达标必须重写。
