# 出片模型档案 · generic（未指定模型时的通用写法）

> ⚠️ 初始档案，待实测校准。老板测完某模型后直接改对应档案文件，热加载即生效。

- 单次生成时长：按 8-10s/块 估算拆块（总时长 ÷ 8-10 向上取整）
- 写法：纯视觉可见描述为主，长叙事散文体；避免抽象情绪词
- 台词/音频：默认不写进提示词（多数模型不支持原生对白生成）；台词靠后期配
- 负向词：段尾保留 Negative 行（部分模型支持参数化负向词，文本内联是兜底）
