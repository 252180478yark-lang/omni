schema_version: 1
feature_id: prompt-lab
title: Prompt 实验室
domain: agent.prompt
owner: {kind: team, id: omni}
lifecycle: active
routes:
  canonical: /prompt-lab
  visible: true
  placements: [direct]
  owned_surfaces: [/prompt-lab]
ia:
  mode: development
  primary_group: prompt-eval
  primary_order: 0
  contextual_groups: []
  phase: retain
  flag: unified_shell
capabilities:
  - {capability_id: prompt-inspection, kind: read}
expected_edges: []
checks:
  - check_id: knowledge-health
    kind: health
    target: /api/v1/system/health/features/prompt-lab
    verifies: health_registration:service:knowledge-engine
    required: true
aliases: []
dependencies:
  - {ref: health_registration:service:knowledge-engine, required: true}
  - {ref: health_registration:service:ai-provider-hub, required: false}
