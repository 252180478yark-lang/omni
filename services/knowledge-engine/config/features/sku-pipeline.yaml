schema_version: 1
feature_id: sku-pipeline
title: SKU Pipeline
domain: commerce.pipeline
owner: {kind: team, id: omni}
lifecycle: active
routes:
  canonical: /sku-pipeline
  visible: true
  placements: [sidebar, direct]
capabilities:
  - {capability_id: sku-pipeline, kind: generate}
expected_edges: []
checks:
  - check_id: knowledge-health
    kind: health
    target: /api/v1/system/health/features/sku-pipeline
    verifies: health_registration:service:knowledge-engine
    required: true
aliases: []
dependencies:
  - {ref: health_registration:service:knowledge-engine, required: true}
  - {ref: health_registration:service:ai-provider-hub, required: true}
