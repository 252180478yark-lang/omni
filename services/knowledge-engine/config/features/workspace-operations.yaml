schema_version: 1
feature_id: workspace-operations
title: 工作台与运营闭环
domain: operations
owner: {kind: team, id: omni}
lifecycle: active
routes:
  canonical: /workspace
  visible: true
  placements: [sidebar, direct]
  owned_surfaces: [/workspace, /decisions, /insights, /review]
ia:
  mode: work
  primary_group: production
  primary_order: 0
  contextual_groups:
    - {mode: development, group: system, order: 0}
  phase: merge
  flag: unified_shell
capabilities:
  - {capability_id: operations-workspace, kind: read}
expected_edges: []
checks:
  - check_id: knowledge-health
    kind: health
    target: /api/v1/system/health/features/workspace-operations
    verifies: health_registration:service:knowledge-engine
    required: true
aliases:
  - {href: /, target: /workspace}
dependencies:
  - {ref: health_registration:service:knowledge-engine, required: true}
