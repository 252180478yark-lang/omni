schema_version: 1
feature_id: model-management
title: 模型与 Provider
domain: system.models
owner: {kind: team, id: omni}
lifecycle: active
routes:
  canonical: /models
  visible: true
  placements: [direct]
  owned_surfaces: [/models]
ia:
  mode: development
  primary_group: prompt-eval
  primary_order: 30
  contextual_groups: []
  phase: retain
  flag: unified_shell
capabilities:
  - {capability_id: model-inspection, kind: read}
expected_edges: []
checks:
  - check_id: knowledge-health
    kind: health
    target: /api/v1/system/health/features/model-management
    verifies: health_registration:service:knowledge-engine
    required: true
aliases: []
dependencies:
  - {ref: health_registration:service:knowledge-engine, required: true}
  - {ref: health_registration:service:ai-provider-hub, required: false}
