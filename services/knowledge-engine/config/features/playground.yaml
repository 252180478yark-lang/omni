schema_version: 1
feature_id: playground
title: Skills 与工具试验场
domain: agent.tools
owner: {kind: team, id: omni}
lifecycle: active
routes:
  canonical: /playground
  visible: true
  placements: [direct]
  owned_surfaces:
    - /playground
    - /playground/reverse-storyboard
ia:
  mode: development
  primary_group: skills-tools
  primary_order: 0
  contextual_groups: []
  phase: retain
  flag: unified_shell
capabilities:
  - {capability_id: tool-playground, kind: admin}
expected_edges: []
checks:
  - check_id: knowledge-health
    kind: health
    target: /api/v1/system/health/features/playground
    verifies: health_registration:service:knowledge-engine
    required: true
aliases: []
dependencies:
  - {ref: health_registration:service:knowledge-engine, required: true}
  - {ref: health_registration:service:ai-provider-hub, required: false}
