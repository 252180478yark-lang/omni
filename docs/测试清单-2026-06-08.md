# omni 测试清单 · 2026-06-08

> 本轮交付：① 人群包投前诊断+提纯（tool+skill）② 巨量云图标签体系 KB ③ omni-desktop 全量多模态 ④ L0 可靠性（worktree）。
> 标 ✅ = 我已验证（你可复验）；标 ⬜ = 需你测（尤其客户端 GUI，先装 0.2.9）。
> 最该先测：**C1 → C2/C4 → C6**（这三条过了多模态主链就通）。

---

## A. 人群包投前诊断 + 提纯（后端已上线，现在就能测）

- [x] **A1** 内置范例诊断：说「诊断一下 地域寻味人 这个包」→ 价值=正向偏离高端 / 购买=更软 / 需求重叠=强 / **定位=种草型** + 四段诊断卡 ✅
- [x] **A2** 提纯施工单：说「提纯一下 地域寻味人」→ 三刀法，每刀落到真实标签（数据工厂→电商品类成交偏好→食用油调味油）✅
- [x] **A3** polish 小白叙事：说「把诊断写细点 / 给我能落地的打法」→ 小白可操作步骤 + 内容打法标 `[KB]`/`[行业推理]` + 每条带"要证实需对比 CTR/CVR" ✅
- [ ] **A4** 新包诊断：从巨量云图导出 CSV → 丢进 `E:\agent\omni\services\knowledge-engine\config\audience\` → 说「诊断一下 <文件名> 这个包」→ 正常出诊断卡，显示名取自 CSV 表头 ⬜
- [x] **A5** 错误处理：说「诊断一下 不存在的包xyz」→ 返 `candidate_not_found` + 内置包清单 + dropbox 路径 ✅
- [ ] **A6** skill 自动触发：随口说「帮我看看 X 这个包适不适合投」→ 自动走 audience-pack-diagnosis skill，每步停等反馈 ⬜

> 命令复验：`docker exec omni-knowledge-engine python -m pytest tests/test_audience_pack_diagnose.py -q` → **15 passed**

---

## B. 巨量云图标签体系 KB（已入库，喂圈包/提纯用）

- [x] **B1** 标签树召回：search_kb 查「数据工厂 标签工厂 食用油调味油」→ top 命中「巨量云图标签体系」字典节，含 `粮油米面…>食用油调味油` ✅（score 1.0）
- [ ] **B2** 圈包 SOP 小白化：走 sku-pipeline step4 / 说「圈一个高端发酵调味品种草人群」→ SOP 给**精确勾选路径**（数据工厂→一级→二级→勾标签）+ 大白话理由，不再笼统 ⬜
- [x] **B3** 诊断 polish 引用标签体系：A3 跑通时看提纯卡 → 引用真实数据工厂品类路径（已含「食用油调味油」）✅

---

## C. omni-desktop 全量多模态（⚠️ 先装 0.2.9 再测，全部你测）

> 装：关掉旧 omni-desktop → 双击 `E:\agent\omni-desktop\release\0.2.9\omni Setup 0.2.9.exe`（或免装跑 `release\0.2.9\win-unpacked\omni.exe`）→ 打开
> ⚠️ 多模态在 **omni-desktop 客户端**里，不是 `启动 omni.bat` 起的那个 web /chat（localhost:3000，另一套代码）。

- [ ] **C1** 回归·纯文字照常：不带附件正常聊一句 → 跟以前一样，无异常 ⬜
- [ ] **C2** 图片上传：点上传按钮选张图 + 说「描述这张图」→ claude 真"看到"图并描述 ⬜
- [ ] **C3** 拖拽上传：拖张图进输入框 → 出虚线高亮 + 附件 chip，发送后 claude 看到 ⬜
- [ ] **C4** 粘贴截图：截图（Win+Shift+S）→ 输入框 Ctrl+V → 截图作为附件上传，claude 看到 ⬜
- [ ] **C5** CSV 上传诊断：上传人群包 CSV + 说「诊断这个包」→ claude 读出 CSV → 走 diagnose_audience_pack ⬜
- [ ] **C6** 生成图内联：说「给我出张分镜图」→ 生成的图**内联缩略图显示**（不再只回 url 文字）⬜
- [ ] **C7** 生成视频内联：出个视频段（generate_video_segments）→ 内联 `<video>` 播放器，可播 ⬜
- [ ] **C8** 下载按钮：悬停生成的图/视频 → 出下载按钮，点→原生保存框落盘 ⬜
- [ ] **C9** Lightbox 全屏：点内联图片 → 全屏大图预览，可关 ⬜
- [ ] **C10** 上传图预览渲染：上传图后看输入区/气泡 → 缩略图能显示（omni-file:// 生效，不是裂图）⬜

---

## D. 后端健康 / 回归

- [x] **D1** doctor 全绿：`docker exec omni-knowledge-engine bash -c "cd /app && PYTHONPATH=/app OMNI_CLAUDE_MD_PATH=/app/CLAUDE.md python -m app.mcp.doctor"` → **all 77 ok** + 33 prompts + /mcp 200 + doc 77==77 + 结论全绿 ✅
- [x] **D2** 新工具单测：`docker exec omni-knowledge-engine python -m pytest tests/test_audience_pack_diagnose.py -q` → 15 passed ✅
- [ ] **D3** KE 整体没崩：客户端正常对话 + 调几个老工具（query_costs / get_sku）→ 一切照常 ⬜

---

## E. L0 可靠性（worktree 分支 `d0f1439`，主树未启用 — 可选，要 merge 后才测）

- [ ] **E1** cookie 失效告警：merge 后 cookie 过期时跑落库 → 日志显式 WARN + `result.auth_failures` 列出失效平台（不再静默）
- [ ] **E2** stream-json 契约：`cd services/knowledge-engine && pytest tests/test_streamjson_contract.py -q` → 33 passed

---

## 部署状态速查

| 交付 | 部署 | 怎么生效 |
|---|---|---|
| 人群包诊断 + 提纯 | ✅ 已上线 | KE 已重启，现在就能用 |
| 巨量云图标签体系 KB | ✅ 已入库 | 已生效 |
| omni-desktop 多模态 | 📦 已打包 0.2.9 | **你装 `omni Setup 0.2.9.exe` 才生效** |
| L0 可靠性 | 🔀 worktree | 待你 review/merge `d0f1439` |

> 所有代码改动**未 commit**，留你审。要拆成干净的主题提交跟我说。
